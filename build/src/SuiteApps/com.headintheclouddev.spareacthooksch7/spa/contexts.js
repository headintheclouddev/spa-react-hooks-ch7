import { ContextType } from '@uif-js/core';
export const ThemeContext = ContextType.new('theme-context'); // ch 5 section 2
export const StateContext = ContextType.new('state-context'); // ch 5 section 3
